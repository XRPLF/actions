name: Run pre-commit hooks

on:
  workflow_call:
    inputs:
      runs_on:
        description: Runner to run the job on
        required: true
        type: string

      container:
        description: "The container object as a JSON string (leave empty to run natively)"
        required: true
        type: string

jobs:
  run-hooks:
    runs-on: ${{ inputs.runs_on }}
    container: ${{ inputs.container != '' && fromJson(inputs.container) || null }}

    steps:
      - name: Checkout Repo ⚡️
        uses: actions/checkout@08eba0b27e820071cde6df949e0beb9ba4906955 # v4.3.0
        with:
          fetch-depth: 0

      - name: Prepare runner 🚀
        uses: XRPLF/actions/.github/actions/prepare_runner@6324e13ca86fa42106a729ef597b43e80d6d1fdb
        with:
          disable_ccache: true

      - name: Run pre-commit ✅
        run: pre-commit run --all-files
